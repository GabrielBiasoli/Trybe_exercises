const stringToUpperCase = (str) =>  str.toUpperCase();
const firstLetterOf = (str) =>  str[0];
const concatTwoStrings = (str1, str2) => str1+str2;

module.exports = {stringToUpperCase, firstLetterOf, concatTwoStrings}